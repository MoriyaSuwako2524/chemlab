[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chemlab"
version = "0.1.0"
description = "Utilities for Q-Chem workflows, ML datasets, and scanning"
authors = [
    { name="Zexuan Wei", email="suwako2524@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["numpy"]

[tool.setuptools.packages.find]
where = ["."]
